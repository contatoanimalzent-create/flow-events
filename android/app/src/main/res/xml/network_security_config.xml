<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Production-grade network security:
        - Bloqueia HTTP (cleartext) em todos os hosts
        - Permite somente HTTPS para dominios da Pulse
        - Trust certificados do sistema (sem certificate pinning para flexibilidade)
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">pulse.animalzgroup.com</domain>
        <domain includeSubdomains="true">animalzgroup.com</domain>
        <domain includeSubdomains="true">supabase.co</domain>
        <domain includeSubdomains="true">stripe.com</domain>
        <domain includeSubdomains="true">cloudinary.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Debug builds aceitam localhost para hot-reload -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
